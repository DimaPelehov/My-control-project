type Props = { addArticleToFavorites: (id: number, count: number) => void }

const AliquamSubcategoryPage = ({ addArticleToFavorites }: Props) => {
    return <div>AliquamSubcategoryPage</div>
}

export default AliquamSubcategoryPage
